{
    "name": "Energy Dashboard",
    "dockerFile": "Dockerfile",
    "settings": {
      "terminal.integrated.shell.linux": "/bin/bash"
    },
    "extensions": [
      "ms-python.python",
      "dbaeumer.vscode-eslint"
    ],
    "postCreateCommand": "pip install flask pandas && npm install -g create-react-app"
  }
  